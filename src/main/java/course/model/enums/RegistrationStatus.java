package course.model.enums;

public enum RegistrationStatus {
    NOT_STARTED, // Chưa mở đăng ký
    OPEN, // Đang mở đăng ký
    CLOSED, // Đã đóng đăng ký
    FULL // Đã đầy (nếu có giới hạn số lượng)
}
